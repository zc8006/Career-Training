---
tags:
  - java
  - interview
  - review
status: active
updated: 2026-09-22
---

# 福州小厂 Java 30 题训练进度

[[Java/Java_MOC|← Java Knowledge Map]]

## 训练定位

- 原始范围：福州小厂 Java 30 道基础面试题。
- 当前阶段：基础保持 + 一层实战细节。
- 每轮固定 12 题：8 道基础题 + 4 道实战细节题。
- 基础题每轮只从原始 30 道题中随机抽取 8 道，同一轮不重复；不按固定顺序出题，也不把新实战题混入基础题。
- 一次只问一道，每个知识点最多追问一层，不问框架或 JVM 源码。
- 实战题实行过关门槛：先复测上一轮未完全掌握的实战题；整组全部独立达到 A 后，才允许进入新一轮实战题。
- 没有直接做过的 Java 生产场景，按“经验边界 + 原理 + 常见方案 + 排查思路”回答，不编造经历。

## 截至第十二轮的结论

- 30 道基础题已经完成首轮学习，并持续进行独立回炉。
- Java 基础、Spring、MyBatis、MySQL、Redis、JVM 和 MuleSoft 项目表达已经形成基本框架。
- 目前主要问题不是完全不会，而是实战细节容易漏项，尤其是 API 名称、参数、返回值判断和完整处理流程。
- 当前重点：线程池、ACID、`tryLock`、`EXPLAIN`、Redis 分布式锁提前过期。

## 已形成的稳定能力

- Java：基本类型、`==/equals()`、`equals/hashCode` 约定、集合选择、线程创建、JVM 内存区域。
- Spring：自动装配、常用注解、事务失效场景、Bean 生命周期、`synchronized/Lock` 的选择。
- MyBatis/MySQL：`#{}` 与 `${}`、模糊查询、事务隔离级别、慢 SQL 排查、联合索引最左前缀原则。
- Redis：缓存穿透/击穿/雪崩、Cache Aside、分布式锁基本加锁和安全解锁。
- 项目表达：PHP REST/SOAP API 迁移到 MuleSoft，能够说明架构、职责及 WSDL/Namespace 兼容问题。

## 第十一轮（2026-09-14）

### 结果

| 类型 | 等级 | 题目 | 主要问题 |
| --- | --- | --- | --- |
| 基础 | A | Java 创建线程 | 四种方式和线程池优势完整 |
| 基础 | C | 事务 ACID | 首答与隔离级别混淆；引导后能复述四个名称 |
| 基础 | A | Spring Boot 自动装配 | 条件创建和用户 Bean 优先正确 |
| 基础 | B | MyBatis 参数处理 | 概念正确，但 SQL 漏写表名 |
| 基础 | A | JVM 内存区域 | 五个区域及私有/共享分类正确 |
| 基础 | A | Redis 缓存三大问题 | 定义和解决方案完整 |
| 基础 | A | MySQL 慢 SQL 排查 | 慢日志、执行计划及索引失效原因完整 |
| 基础 | A | MuleSoft 项目架构 | 迁移范围、处理流程和职责清楚 |
| 实战 | B | `tryLock` | 缺时间单位、返回值判断及安全解锁条件 |
| 实战 | B | `EXPLAIN` | 漏 `possible_keys` 和 `Extra` |
| 实战 | B | Redis 锁提前过期 | 漏 watchdog 自动续期和业务幂等 |
| 实战 | C | 线程池参数与流程 | 七个参数和执行流程首次学习后仍未记住 |

汇总：基础 A 6、基础 B 1、基础 C 1；实战 B 3、实战 C 1。

## 第十二轮（2026-09-15）

### 结果

| 类型 | 等级 | 题目 | 主要表现 |
| --- | --- | --- | --- |
| 基础 | B | 事务 ACID | 原子性、一致性、隔离性正确，首次漏持久性；提示后补全 |
| 基础 | A | MyBatis 参数处理 | 区别和包含式模糊查询完整，恢复基础 A |
| 基础 | A | `equals/hashCode` | 能说明相等约定及 HashMap/HashSet 后果 |
| 基础 | A | `@Transactional` 失效 | 独立说出五种常见情况 |
| 基础 | A | Bean 生命周期 | 主线及初始化前后处理完整 |
| 基础 | A | 事务隔离级别 | 四种级别和 InnoDB 默认值正确 |
| 基础 | A | `synchronized/Lock` | 区别和项目选择正确 |
| 基础 | A | MuleSoft SOAP 迁移难点 | Namespace 处理和新旧验证清楚 |
| 实战 | C | 线程池参数与流程 | 首答忘记；提示后记住执行顺序，七个参数仍需复测 |
| 实战 | B | `tryLock` | 只先答出方法；提示后理解返回 `false` 不能解锁 |
| 实战 | B | `EXPLAIN` | 再次只答出 `type`、`key`、`rows`；提示后补齐两项 |
| 实战 | B | Redis 锁提前过期 | 风险、过期时间和幂等正确，仍漏 watchdog 自动续期 |

汇总：基础 A 7、基础 B 1、基础 C 0；实战 B 3、实战 C 1。

## 第十二轮结束时 B/C 清单（历史）

### 基础 B

- ACID：下次必须独立完整说明原子性、一致性、隔离性和持久性的含义。

### 实战 B

- `tryLock`：`tryLock(2, TimeUnit.SECONDS)`；检查返回值；只有成功获得锁才在 `finally` 中 `unlock()`。
- `EXPLAIN`：一次说全 `type`、`possible_keys`、`key`、`rows`、`Extra`。
- Redis 锁提前过期：补齐 watchdog 自动续期，并说明业务幂等和安全解锁。

### 实战 C

- 线程池：先独立掌握“核心线程 → 任务队列 → 最大线程 → 拒绝策略”，再逐步补齐七个参数。

## 核心记忆

1. ACID 是原子性、一致性、隔离性、持久性。
2. 持久性是事务提交后结果永久保存，系统重启也不应丢失。
3. `tryLock(2, TimeUnit.SECONDS)` 返回 `true` 后才能执行业务和解锁。
4. `tryLock()` 返回 `false` 时，当前线程没有获得锁，不能调用 `unlock()`。
5. `EXPLAIN` 关注 `type`、`possible_keys`、`key`、`rows`、`Extra`。
6. 线程池处理顺序是核心线程、任务队列、最大线程、拒绝策略。
7. 线程池七个参数包括核心线程数、最大线程数、存活时间、时间单位、任务队列、线程工厂和拒绝策略。
8. Redis 锁提前过期可能导致多个线程同时执行业务。
9. Redis 分布式锁可通过合理过期时间、自动续期和业务幂等降低风险。
10. MyBatis 包含式模糊查询使用 `LIKE CONCAT('%', #{param}, '%')`。

## 第十三轮计划（已执行）

仍执行 12 题制，一次一道：

1. 8 道基础题先独立复测 ACID 完整含义，再从原 30 题轮换 7 道。
2. 4 道实战题依次复测：线程池执行流程、`tryLock` 完整处理、`EXPLAIN` 五个字段、Redis 锁提前过期。
3. 线程池本轮先保证执行顺序独立正确，再补参数，避免一次背诵过多。
4. 实战题只到项目使用级，不问源码。

## 第十三轮（2026-09-16）

### 结果

| 类型 | 等级 | 题目 | 主要表现 |
| --- | --- | --- | --- |
| 基础 | A | 事务 ACID | 四个特性及含义完整，恢复基础 A |
| 基础 | A | Spring AOP | 动态代理、JDK/CGLIB 区别及 final 限制正确 |
| 基础 | A | Cache Aside | 查询回填、更新数据库后删除缓存的顺序正确 |
| 基础 | A | ArrayList/LinkedList | 数据结构、访问方式和选择场景正确 |
| 基础 | B | Spring Boot 常用注解 | 用途正确，但 `@Transactional` 拼写不准确 |
| 基础 | A | Redis 缓存三大问题 | 穿透、击穿、雪崩及处理方案完整 |
| 基础 | A | JVM 内存区域 | 五个区域及私有/共享分类正确 |
| 基础 | A | Java 创建线程 | Thread、Runnable、Callable/FutureTask、线程池完整 |
| 实战 | C | 线程池执行流程 | 首答忘记；引导后记住核心线程、队列、最大线程、拒绝策略 |
| 实战 | B | `tryLock` | 理解成功后才能解锁，但完整流程表述仍不稳定 |
| 实战 | B | `EXPLAIN` | 能说出主要字段，仍漏 `Extra` 或字段含义不够精确 |
| 实战 | B | Redis 锁提前过期 | 风险和主要方案正确，完整性仍需复测 |

汇总：基础 A 7、基础 B 1、基础 C 0；实战 A 0、实战 B 3、实战 C 1。

### 下一轮原定重点

- 基础题继续维持 8 道回炉。
- 实战重点回炉：线程池执行流程、`tryLock` 完整写法、`EXPLAIN` 五个字段、Redis 分布式锁提前过期。

## 第十四轮（2026-09-17）

### 结果

| 类型 | 等级 | 题目 | 主要表现 |
| --- | --- | --- | --- |
| 基础 | A | `int/Integer` | 基本类型、包装类、泛型、装拆箱及 null 拆箱 NPE 正确 |
| 基础 | A | `==/equals/hashCode` | 比较方式、重写约定及 HashMap/HashSet 后果完整 |
| 基础 | A | `@Transactional` 失效 | 独立说出 private、内部调用、异常被捕获、异常类型、手动 new Bean |
| 基础 | A | MyBatis 参数处理 | `#{}`、`${}` 和安全模糊查询正确 |
| 基础 | A | Bean 生命周期 | 主线顺序完整，Aware 名称经拼写纠正 |
| 基础 | A | 事务隔离级别 | 四种级别、默认 RR 及三类读问题完整 |
| 基础 | A | JVM 内存区域 | 五个区域、私有/共享、对象和引用位置完整 |
| 基础 | A | Spring Boot 自动配置 | 条件创建及用户 Bean 优先正确 |
| 实战 | B | 线程池执行流程 | 拆分提示后完成 2 核心、2 入队、2 非核心、1 拒绝的场景题 |
| 实战 | B | `tryLock` | 核心方向正确，但“已获得锁”和 false 分支表述仍不够准确 |
| 实战 | B | `EXPLAIN` | `type/key/possible_keys/rows` 正确，仍漏 `Extra` 且含义容易混写 |
| 实战 | A | Redis 锁提前过期 | 能说明并发重复执行、合理过期时间、watchdog 和业务幂等 |

汇总：基础 A 8、基础 B 0、基础 C 0；实战 A 1、实战 B 3、实战 C 0。

### 本轮规则纠正

- `String`、`StringBuilder`、`StringBuffer` 属于新实战扩展，不属于原 30 道基础题。
- 该题在旧实战回炉尚未全部通过前被提前提出，因此不计入本轮正式实战成绩；暂存为以后待学习题。
- 以后必须先完成上一轮实战回炉，并且整组题全部独立达到 A，才能增加新的实战题。
- 如果任意回炉题需要提示或只达到 B/C，本轮不进入新实战，下一轮继续回炉。

## 第十五轮前待回炉清单（历史）

### 基础

- 当前无 B/C 项；继续从原 30 题中轮换，防止遗忘。

### 实战（必须整组达到 A）

1. 线程池：独立说明“核心线程 → 任务队列 → 最大线程 → 拒绝策略”，并完成简单容量场景计算。
2. `tryLock`：说明 `tryLock(2, TimeUnit.SECONDS)` 的返回值判断、业务执行、false 分支和 `finally` 安全解锁。
3. `EXPLAIN`：一次说全 `type`、`possible_keys`、`key`、`rows`、`Extra` 及各自含义。
4. Redis 锁提前过期：说明重复执行风险、合理过期时间、watchdog、业务幂等和唯一值 + Lua 安全解锁。

### 通过后才能学习

- `String`、`StringBuilder`、`StringBuffer` 的区别与项目选择。
- 其他新实战题暂不加入。

## 第十五轮计划（已执行）

1. 基础部分继续从原 30 题中抽取 8 道，一次一道。
2. 实战部分只复测上述 4 道旧题，不增加新题。
3. 4 道实战题必须在无提示情况下全部达到 A，才算整组通过。
4. 如果整组通过，从下一轮开始加入新的项目使用级实战题；仍不问源码。


## 第十五轮（2026-09-18）

### 结果

| 类型 | 等级 | 题目 | 主要表现 |
| --- | --- | --- | --- |
| 基础 | A | Spring Boot 常用注解 | 能说明启动类、业务层、自动注入、事务及 GET/POST 映射 |
| 基础 | A | Spring AOP | 动态代理、JDK/CGLIB 区别及 final 限制正确 |
| 基础 | A | Bean 生命周期 | 实例化、注入、Aware、初始化前后处理、使用和销毁完整 |
| 基础 | A | `@Transactional` 失效 | private、内部调用、异常被捕获、异常类型和手动 new Bean 完整 |
| 基础 | B | MyBatis 参数处理 | `#{}/${}` 原理正确，但模糊查询 SQL 漏写 `LIKE` |
| 基础 | B | 事务隔离级别 | 四级及三类读问题正确，但漏答 InnoDB 默认 RR |
| 基础 | A | JVM 内存区域 | 五个区域、私有/共享及对象与局部引用位置正确 |
| 基础 | A | Spring Boot 自动配置 | 条件装配及用户 Bean 优先正确 |
| 实战 | A | 线程池执行流程 | 独立完成 2 核心、2 入队、2 非核心、1 拒绝的容量题 |
| 实战 | B | `tryLock` | true/false 与安全解锁正确，但漏中断处理；需明确 true 时已获得锁 |
| 实战 | B | `EXPLAIN` | 五个字段及全表扫描判断基本记住，但字段含义表述不完整 |
| 实战 | B | Redis 锁提前过期 | 重复执行、过期时间、watchdog、幂等正确，漏唯一值 + Lua 防误删 |

汇总：基础 A 6、基础 B 2、基础 C 0；实战 A 1、实战 B 3、实战 C 0。

### 本轮结论

- 基础整体稳定，需回炉 MyBatis 模糊查询的 `LIKE` 和 InnoDB 默认隔离级别 RR。
- 线程池执行流程首次独立达到 A。
- 实战整组没有全部达到 A，因此不能加入新实战题。
- 下一轮仍复测线程池、`tryLock`、`EXPLAIN` 和 Redis 锁提前过期四题。

## 第十五轮后待回炉清单（历史）

### 基础 B

1. MyBatis：包含式模糊查询写成 `WHERE name LIKE CONCAT('%', #{param}, '%')`。
2. MySQL：InnoDB 默认隔离级别是可重复读（REPEATABLE READ）。

### 实战（整组必须全部达到 A）

1. 线程池：继续独立完成“核心线程 → 任务队列 → 非核心线程 → 拒绝策略”的容量场景题，保持 A。
2. `tryLock`：返回 true 表示已经获得锁；false 不执行业务也不解锁；处理中断并恢复中断标记；只在成功持锁时于 `finally` 解锁。
3. `EXPLAIN`：准确说明 `type`、`possible_keys`、`key`、`rows`、`Extra` 的含义；识别 `type=ALL`、`key=NULL`、`rows` 很大的风险。
4. Redis 锁提前过期：说全重复执行风险、合理过期时间、watchdog、业务幂等、唯一值和 Lua 原子安全解锁。

## 第十六轮计划（已执行）

1. 基础部分继续抽取 8 道，优先复测本轮两道 B 题。
2. 实战部分仍只复测上述 4 道旧题，不增加新题。
3. 4 道实战题必须在无提示情况下全部达到 A，下一轮才允许加入新的项目使用级实战题。
4. 继续坚持一次一道、实战级、不问源码。


## 第十六轮（2026-09-21）

### 结果

| 类型 | 等级 | 题目 | 主要表现 |
| --- | --- | --- | --- |
| 基础 | A | MyBatis 参数处理 | 独立说清 `#{}/${}`，包含式模糊查询 SQL 完整 |
| 基础 | A | 事务隔离级别 | 四级、默认 RR、脏读、不可重复读和幻读完整 |
| 基础 | A | 事务 ACID | 四个特性及含义完整 |
| 基础 | A | ArrayList/LinkedList | 底层结构、项目选择及适用场景正确 |
| 基础 | A | `==/equals/hashCode` | 比较方式、重写约定及哈希集合后果完整 |
| 基础 | B | Cache Aside | 查询与更新顺序正确，“为什么删除而不更新缓存”解释不够准确 |
| 基础 | A | `synchronized/Lock` | 自动/手动释放、选择场景及 finally 解锁正确 |
| 基础 | B | Redis 缓存三大问题 | 击穿和雪崩正确；穿透漏掉“数据库中也不存在”这一关键条件 |
| 实战 | A | 线程池执行流程 | 独立完成容量题，并说明是否抛异常取决于拒绝策略 |
| 实战 | B | `tryLock` | true/false 和安全解锁正确，再次漏答中断处理 |
| 实战 | A | `EXPLAIN` | 在目标面试深度下能说明核心字段及全表扫描判断 |
| 实战 | A | Redis 锁提前过期 | 重复执行、误删锁、watchdog、幂等和锁归属判断完整 |

汇总：基础 A 6、基础 B 2、基础 C 0；实战 A 3、实战 B 1、实战 C 0。

### 深度规则纠正

- `EXPLAIN` 属于常见项目排查题，不按源码题训练。
- 福州小厂普通 Java 岗只要求掌握：`type`、`possible_keys`、`key`、`rows`、`Extra` 的基本用途，以及 `type=ALL`、`key=NULL`、`rows` 很大的风险。
- `Using filesort`、`Using temporary` 只作为加分项，不要求强行背诵。
- 后续实战题继续限制在“常见面试 + 项目能用”的层级，不扩展成源码或冷门字段记忆题。

## 第十六轮后待回炉清单（历史）

### 基础 B

1. Cache Aside：能说明“先更新数据库，再删除缓存”，并解释直接更新缓存可能因并发写入顺序、复杂计算和无效更新造成问题。
2. 缓存穿透：必须明确请求的数据在缓存和数据库中都不存在；解决方案是参数校验、缓存空值和布隆过滤器。

### 实战（整组必须全部达到 A）

1. 线程池：保持“核心线程 → 队列 → 非核心线程 → 拒绝策略”的容量计算能力。
2. `tryLock`：必须完整说出 true、false、`InterruptedException` 和 finally 安全解锁四个分支；中断时恢复中断标记。
3. `EXPLAIN`：保持核心字段和全表扫描判断，不再要求背冷门细节。
4. Redis 锁提前过期：保持重复执行、误删锁、合理过期时间、watchdog、幂等和锁归属判断的完整回答。

## 第十七轮计划（已执行）

1. 基础部分继续8题，优先复测 Cache Aside 和缓存穿透。
2. 实战部分仍复测原4题；由于 `tryLock` 为 B，暂不增加新实战题。
3. `tryLock` 重点只补一句：捕获 `InterruptedException` 后调用 `Thread.currentThread().interrupt()`，再结束或向上抛出。
4. 继续一次一道，只训练正常面试会问、项目中能用的内容，不问源码。


## 第十七轮（2026-09-21）

### 结果

| 类型 | 等级 | 题目 | 主要表现 |
| --- | --- | --- | --- |
| 基础 | A | Cache Aside | 查询回填、更新数据库后删除缓存及不直接更新缓存的原因完整 |
| 基础 | A | Redis 缓存三大问题 | 穿透、击穿、雪崩的定义和处理方案完整 |
| 基础 | A | `int/Integer` | 基本类型、包装类、默认值、泛型、装拆箱和 null 风险完整 |
| 基础 | A | Bean 生命周期 | 实例化到销毁的主线顺序完整 |
| 基础 | A | Java 创建线程 | Thread、Runnable、Callable/FutureTask 和线程池完整 |
| 基础 | A | Spring AOP | 动态代理、JDK/CGLIB 及 final 限制正确 |
| 基础 | A | `@Transactional` 失效 | 分两次补全 private、内部调用、异常捕获、异常类型、手动 new Bean |
| 基础 | A | JVM 内存区域 | 五个区域、线程私有/共享及对象与局部引用位置正确 |
| 实战 | A | 线程池执行流程 | 独立完成容量题，并说明默认拒绝策略抛异常但可配置 |
| 实战 | A | `tryLock` | true、false、中断恢复和 finally 安全解锁四个分支全部说全 |
| 实战 | A | `EXPLAIN` | 按项目排查级要求说明核心字段及全表扫描判断 |
| 实战 | B | Redis 锁提前过期 | 风险、过期时间、watchdog 和幂等正确，漏唯一值 + Lua 安全解锁 |

汇总：基础 A 8、基础 B 0、基础 C 0；实战 A 3、实战 B 1、实战 C 0。

### 本轮结论

- 基础8题全部达到 A，基础知识整体稳定。
- `tryLock` 首次把中断处理补全，恢复为 A。
- Redis 锁提前过期能够说明主要风险和方案，但安全解锁只说明了误删风险，没有主动说出唯一值校验和 Lua 原子删除。
- 实战整组没有全部达到 A，因此下一轮仍不增加新实战题。

## 当前待回炉清单（第十七轮后）

### 基础

- 当前无 B/C 项；继续从原30题轮换，防止遗忘。

### 实战（整组必须全部达到 A）

1. 线程池：保持“核心线程 → 队列 → 非核心线程 → 拒绝策略”的容量计算能力。
2. `tryLock`：保持 true、false、`InterruptedException`、finally 安全解锁四个分支的完整回答。
3. `EXPLAIN`：保持核心字段和全表扫描判断，只按常见项目排查级要求训练。
4. Redis 锁提前过期：必须主动说出“加锁保存唯一标识；解锁校验标识；Lua 原子执行判断和删除”，同时说明合理过期时间、watchdog 和业务幂等。

## 第十八轮计划（已执行）

1. 基础部分继续从原30题轮换8题。
2. 实战部分仍复测原4题；优先检查 Redis 安全解锁是否能独立说全。
3. 实战4题全部达到 A 后，下一轮才加入新的项目使用级实战题。
4. 继续一次一道，只训练常见面试和项目能用的内容，不问源码。

## 第十八轮（2026-09-22）

### 结果

| 类型 | 等级 | 题目 | 主要表现 |
| --- | --- | --- | --- |
| 基础 | A | `==/equals/hashCode` | 基本类型值比较、对象身份、String 内容比较及哈希约定完整 |
| 基础 | A | ArrayList/LinkedList | 底层结构和场景选择正确；中间操作应说插入删除，定位后再操作 |
| 基础 | A | `int/Integer` | 基本类型、包装类、装拆箱、泛型和 null 拆箱风险完整 |
| 基础 | A | 事务 ACID | 四项含义正确，末项把“持久性”口误为“一致性” |
| 基础 | B | MyBatis 参数处理 | 区别正确，但模糊查询 SQL 误用 `${param}`；应使用 `#{param}` |
| 基础 | A | `@Transactional` 失效 | private、自调用、捕获异常、回滚异常类型、手动 new 对象完整 |
| 基础 | A | Redis 缓存三大问题 | 穿透、击穿、雪崩的定义及解决方式完整 |
| 基础 | B | Spring Boot 自动配置 | 条件创建正确；误认为用户有同类型 Bean 就必定跳过 |
| 实战 | A | 线程池执行流程 | 2 核心、2 入队、2 非核心、第7个拒绝；默认 AbortPolicy 抛异常 |
| 实战 | A | `tryLock` | true/false、中断恢复及成功持锁后 finally 解锁完整 |
| 实战 | A | `EXPLAIN` | type、possible_keys、key、rows、Extra 和 ALL/NULL 判断完整 |
| 实战 | A | Redis 锁提前过期 | 并发重复执行、误删他人锁、幂等、续期、唯一值 + Lua 原子解锁完整 |

汇总：基础 A 6、B 2、C 0；实战 A 4、B 0、C 0。本轮原4道实战回炉题全部独立达到 A，下一轮可以进入新的项目使用级实战题。

### 基础纠错

1. MyBatis 模糊查询：`WHERE name LIKE CONCAT('%', #{param}, '%')`；用户输入不使用 `${param}` 拼接。
2. Spring Boot 自动配置：只有配置项确实以 `@ConditionalOnMissingBean` 等条件判断，且用户 Bean 符合匹配条件，才会跳过对应自动 Bean；不能概括成“同类型一定跳过”。

## 第十九轮计划

1. 基础题从原始30题中**随机抽8题**，同一轮不重复；每次只问一道，不按固定列表顺序出题。
2. 实战回炉门槛已通过，可以安排新的常见项目场景；此前暂缓的 String、StringBuilder、StringBuffer 区别与选择可以纳入。
3. 新一组实战题仍按“一次一道、整组全部 A 后再换新组”执行；只问正常面试和项目使用深度，不问源码。
4. 如果换到新窗口，以本文件第十八轮结果和第十九轮计划继续，不重做第十八轮的四道实战回炉题。

## Related

- [[Java/Java_MOC]]
- [[01-Career/Career_MOC|Interview & Career]]
- [[MuleSoft/MuleSoft_MOC|MuleSoft]]
